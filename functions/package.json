{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test test/firestore.rules.test.js\"",
    "test:backend": "firebase emulators:exec --only firestore \"node --test test/rap_sheet_snapshot.test.js\"",
    "test": "firebase emulators:exec --only firestore \"node --test test/firestore.rules.test.js test/rap_sheet_snapshot.test.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^7.0.5"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.2.1"
  }
}
